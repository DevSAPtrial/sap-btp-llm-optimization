{
  "name": "sap-btp-llm-optimization",
  "version": "1.0.0",
  "description": "SAP CAP Node.js app integrating GPT-4o via SAP AI Core",
  "scripts": {
    "start": "cds run",
    "deploy": "cds deploy --to sqlite"
  },
  "dependencies": {
    "@sap/cds": "^7",
    "axios": "^1.3.0",
    "node-cache": "^5.1.2"
  },
  "engines": {
    "node": ">=16"
  }
}
